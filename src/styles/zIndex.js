// zIndexes should be declared as vars and then mapped to their useage,
// but we'll take some shortcuts to save time
export const zIndex = {
  background: 100,
  foreground: 200,
  floating: 300,
  overlay: 400,
};
